# 项目手册：北京企业出海活动监控看板

> 本手册记录项目架构、关键决策、踩过的坑和经验教训。下次做类似项目前先通读一遍，避免重复踩坑。

---

## 一、项目概览

**目标**：在现有静态 HTML 看板基础上，增加状态筛选、联网抓取更新、自动导出 Excel、GitHub Pages 在线发布。

**技术栈**：纯静态 HTML + 内嵌 JS / Python 3 (urllib + lxml + openpyxl) / macOS launchd 定时任务 / GitHub Pages 部署 / SSH 认证

**最终架构**：

```
定时任务 (launchd, 周一/三/六 09:00)
  → caffeinate 防休眠
    → Python main.py
      → 加载现有数据 (events.json 或 HTML)
      → 联网抓取 9 个来源 (scraper.py)
      → 合并去重 + 更新状态 (updater.py)
      → 更新 index.html 内嵌数据
      → 导出未举办活动 Excel (exporter.py)
      → git commit + push → GitHub Pages 自动更新
```

**访问地址**：https://freemanwhoelse.github.io/overseas-dashboard/

---

## 二、文件结构说明

```
北京企业出海活动监控看板/
├── index.html                        # GitHub Pages 入口 (= 增强版看板)
├── 北京企业出海活动监控看板.html          # 原始看板 (未改动)
├── 北京企业出海活动监控看板_20260728v1.html # 增强版看板 (开发副本)
├── monitor/
│   ├── config.py                    # 配置: 来源URL、关键词、路径、场地映射
│   ├── scraper.py                   # 联网抓取: 列表页→关键词匹配→详情页提取
│   ├── updater.py                   # 数据更新: HTML解析、合并去重、状态更新、写回
│   ├── exporter.py                  # Excel导出: openpyxl, 带格式和筛选
│   ├── main.py                      # 主入口: 串联全流程 + git自动推送
│   └── run_monitor.sh               # caffeinate包装脚本 (防休眠)
├── data/events.json                 # 活动数据 (运行时生成)
├── exports/                         # Excel输出目录
├── logs/                            # 运行日志
├── com.beijing.dashboard-monitor.plist  # launchd定时任务配置模板
├── install_schedule.sh              # 定时任务安装/卸载脚本
├── deploy.sh                        # GitHub一键部署脚本
├── .gitignore
└── README.md
```

---

## 三、关键决策记录

### 3.1 "未举办"的定义
用户明确：报名中 (confirmed) + 即将举办 (upcoming) 都算"未举办"。筛选标签和 Excel 导出都按此口径。

### 3.2 联网抓取 vs 纯本地导出
选择了方案B（联网抓取9个来源）。代价是维护成本高、依赖网站可访问性。如果只需要"按现有数据导出Excel"，方案A更简单稳定。

### 3.3 部署平台选择
用户朋友在国内访问。讨论了腾讯云COS、阿里云OSS、Gitee Pages等国内方案，最终用户选择GitHub Pages。国内访问偶尔会慢，但免费且自动部署流程简单。

### 3.4 认证方式
无 gh CLI、无 Homebrew、无 credential helper。最终用 SSH 密钥认证，比 HTTPS + token 更适合定时任务场景（不需要交互输入）。

### 3.5 防休眠方案
合盖+电池的 DarkWake 只持续 2-12 秒。用 caffeinate -i 包装脚本，防止运行期间休眠。但 caffeinate 无法保证"被唤醒"——这需要 pmset 或保持盖子打开。

---

## 四、踩过的坑与经验教训

### 坑1: 定时任务创建了安装脚本但从未运行

**现象**：周三早上9点没有更新。
**根因**：只写了 `install_schedule.sh`，但从未执行它。launchd 里没有任务，plist 也没安装到 `~/Library/LaunchAgents/`。
**教训**：**安装脚本写完必须当场运行验证。** 不能假设"脚本写好了就等于功能上线了"。验证方法：
```bash
launchctl list | grep dashboard  # 确认任务已加载
cat ~/Library/LaunchAgents/com.beijing.dashboard-monitor.plist  # 确认plist已安装
```

### 坑2: 合盖+电池下 DarkWake 只持续数秒

**现象**：pmset 日志显示每次 DarkWake 仅 2-12 秒，脚本需要 1-3 分钟，根本跑不完。
**根因**：macOS 在电池+合盖状态下，RTC唤醒后只做极短暂的 DarkWake 就重新休眠。
**解决**：加了 `caffeinate -i` 包装脚本。但前提是 launchd 能成功启动脚本。
**教训**：**定时任务依赖电脑处于可用状态。** 电池+合盖是最差场景。最佳保证：盖子打开 或 插上电源。如果要合盖运行，需要 `sudo pmset repeat wakeorpoweron MWF 09:00:00` 配合 caffeinate。

### 坑3: HTML中 venue 数据只存在于 JS 端

**现象**：Python 解析 HTML 时拿不到举办地址，导出的 Excel 地址列全是"待确认"。
**根因**：原始看板用 JS 的 `venueMap` 在浏览器端动态添加 venue 字段，Python 解析 `const events = [...]` 时看不到这些数据。
**解决**：在 `config.py` 中创建 `VENUE_MAP` 字典，updater 解析后手动补充。
**教训**：**前端动态添加的数据，后端解析时拿不到。** 如果数据需要前后端共享，应该直接写在数据源里，而不是靠 JS 动态注入。

### 坑4: 删除 Perplexity 内联编辑脚本后多了一个 </script>

**现象**：删除 HTML 中嵌入的 Perplexity `data-pplx-inline-edit` 脚本块后，文件末尾多了一个 `</script>` 标签。
**根因**：sed 截取时保留了主脚本的 `</script>`，又额外加了一个。
**解决**：用 apply_patch 删除多余的 `</script>`。
**教训**：**手动编辑 HTML 结构后要检查标签配对。** 可以用 `grep -c '<script' file` 和 `grep -c '</script>' file` 快速验证。

### 坑5: lxml 安装编译时间极长

**现象**：`pip3 install lxml` 卡住超过 90 秒（在从源码编译）。
**解决**：中途放弃安装，改用标准库 `lxml.html` 兜底。后来发现 lxml 其实已经装好了（可能是系统自带的或之前装过）。
**教训**：**安装第三方库前先检查是否已可用**（`python3 -c "import lxml"`），避免重复安装。如果确实需要安装且编译慢，考虑用预编译 wheel（`pip install --only-binary :all: lxml`）。

### 坑6: git init 在沙箱中被拒绝

**现象**：`git init` 报 "Operation not permitted"。
**根因**：沙箱限制了对 `.git` 目录的写入。
**解决**：用 `sandbox_permissions: require_escalated` 提升权限。
**教训**：**git 操作需要写入 `.git` 目录，沙箱环境下需要提权。** 所有 git 相关命令（init、add、commit、push）都需要 escalated 权限。

### 坑7: SSH 密钥生成后首次连接被拒绝

**现象**：生成 SSH 密钥并添加到 GitHub 后，`ssh -T git@github.com` 仍报 "Permission denied (publickey)"。
**根因**：ssh-agent 没有加载密钥（新开的 shell 不会自动加载）。
**解决**：`eval "$(ssh-agent -s)" && ssh-add ~/.ssh/id_ed25519`。同时配置 `~/.ssh/config`（UseKeychain yes）确保持久化。
**教训**：**SSH 密钥生成后必须加载到 agent。** macOS 上用 `ssh-add --apple-use-keychain ~/.ssh/id_ed25519` 存入 Keychain，并配置 `~/.ssh/config` 中 `UseKeychain yes`，这样定时任务的非交互环境也能认证。

### 坑8: HTTPS push 失败，无 credential helper

**现象**：`git push` 报 "could not read Username for 'https://github.com'"。
**根因**：非交互环境下无法输入 GitHub 用户名密码，也没有配置 credential helper。
**解决**：改用 SSH 远程地址 `git@github.com:...`。
**教训**：**定时任务场景必须用非交互认证。** SSH 密钥是首选；如果用 HTTPS，必须配置 credential helper 或 Personal Access Token。

### 坑9: Python 路径在 plist 中写错

**现象**：plist 初始模板用 `/usr/bin/python3`，但实际 openpyxl/lxml 装在 `/Library/Frameworks/Python.framework/Versions/3.14/bin/python3`。
**解决**：先 `which python3` 确认路径，更新 plist。
**教训**：**plist 中的解释器路径必须用绝对路径，且要和依赖库安装的 Python 一致。** macOS 上可能有多个 Python（系统自带、Framework版、Homebrew版），不能假设路径。

---

## 五、下次类似项目的检查清单

### 开发阶段
- [ ] 确认 Python 路径 (`which python3`)，plist 中用绝对路径
- [ ] 确认依赖库装在正确的 Python 下 (`python3 -c "import xxx"`)
- [ ] 前后端共享数据不要只靠 JS 动态注入，数据源里要有完整字段
- [ ] 编辑 HTML 后检查 `<script>` 标签配对
- [ ] 安装第三方库前先检查是否已可用

### 定时任务
- [ ] **install_schedule.sh 写完必须当场运行**
- [ ] 运行后验证: `launchctl list | grep 任务名`
- [ ] 验证 plist 中的 Python 路径、工作目录、环境变量都正确
- [ ] 考虑休眠问题: 用 caffeinate 包装脚本
- [ ] 告知用户: 合盖+电池是最差场景，保持盖子打开或插电源
- [ ] 如果必须合盖运行: `sudo pmset repeat wakeorpoweron MWF 09:00:00`

### Git/GitHub 部署
- [ ] 沙箱环境下 git 操作需要 `require_escalated`
- [ ] 优先用 SSH 认证 (生成密钥 → 添加到 GitHub → 配置 ~/.ssh/config)
- [ ] SSH 密钥存入 macOS Keychain: `ssh-add --apple-use-keychain ~/.ssh/id_ed25519`
- [ ] ~/.ssh/config 配置 UseKeychain yes，确保定时任务非交互环境可认证
- [ ] index.html 作为 GitHub Pages 入口
- [ ] .gitignore 排除 logs/、exports/、__pycache__/
- [ ] main.py 中 git push 只提交 index.html 和 data/events.json
- [ ] 推送后提醒用户在 GitHub Settings → Pages 开启部署

### 测试验证
- [ ] `python3 main.py --test` 验证解析和导出
- [ ] `python3 main.py --export-only` 验证完整流程(不联网)
- [ ] `ssh -T git@github.com` 验证认证
- [ ] `launchctl list | grep 任务名` 验证定时任务
- [ ] 推送后访问 GitHub Pages 链接验证页面正常

---

## 六、运行维护备忘

### 手动运行
```bash
cd /Users/zhouyufeng/Documents/Codex/北京企业出海活动监控看板/monitor
python3 main.py              # 完整流程(联网+导出+推送)
python3 main.py --export-only  # 仅导出(不联网)
python3 main.py --test       # 测试模式(不联网不推送)
python3 main.py --no-push    # 完整流程但不推送GitHub
```

### 定时任务管理
```bash
cd /Users/zhouyufeng/Documents/Codex/北京企业出海活动监控看板
./install_schedule.sh           # 安装
./install_schedule.sh remove    # 卸载
launchctl list | grep dashboard # 查看状态
```

### 查看日志
```bash
ls logs/                        # 列出日志文件
cat logs/monitor_YYYYMMDD.log   # 查看指定日期日志
```

### 检查GitHub Pages
- 仓库: https://github.com/FreemanWhoElse/overseas-dashboard
- 页面: https://freemanwhoelse.github.io/overseas-dashboard/
- Pages设置: 仓库 Settings → Pages → Branch: main / root

### 修改监控来源或关键词
编辑 `monitor/config.py` 中的 `SOURCES` 和 `KEYWORDS`。

### 注意事项
- 定时任务执行时间: 每周一、三、六 09:00
- 执行前提: 电脑盖子打开 或 插上电源
- 抓取依赖目标网站可访问性，政府网站偶尔会改版
- 日志在 `logs/` 目录，用于排查问题
```
```
